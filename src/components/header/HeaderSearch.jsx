const HeaderSearch = ({ placeholder }) => (
    <input
        className="w-md h-12 bg-dark-grey rounded-full px-4 font-light"
        type="search"
        placeholder={placeholder}
    />
);

export default HeaderSearch;
